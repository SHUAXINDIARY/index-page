diff --git a/esm/systems/DrawPencil.js b/esm/systems/DrawPencil.js
--- a/esm/systems/DrawPencil.js
+++ b/esm/systems/DrawPencil.js
@@ -77,13 +77,13 @@
             }
             if (input.pointerUpTrigger) {
                 const { brush } = this.selections.get(camera.__id);
+                // Just click on the canvas, do nothing
+                if (!brush || ((_a = brush.points) === null || _a === void 0 ? void 0 : _a.length) === 0) {
+                    return;
+                }
                 if (isBrowser &&
                     brush.visibility === 'hidden') {
                     return;
                 }
-                // Just click on the canvas, do nothing
-                if (!brush || ((_a = brush.points) === null || _a === void 0 ? void 0 : _a.length) === 0) {
-                    return;
-                }
                 api.runAtNextTick(() => {
                     api.updateNode(brush, { visibility: 'hidden' }, false);
diff --git a/lib/systems/DrawPencil.js b/lib/systems/DrawPencil.js
--- a/lib/systems/DrawPencil.js
+++ b/lib/systems/DrawPencil.js
@@ -83,13 +83,13 @@
             }
             if (input.pointerUpTrigger) {
                 const { brush } = this.selections.get(camera.__id);
+                // Just click on the canvas, do nothing
+                if (!brush || ((_a = brush.points) === null || _a === void 0 ? void 0 : _a.length) === 0) {
+                    return;
+                }
                 if (utils_1.isBrowser &&
                     brush.visibility === 'hidden') {
                     return;
                 }
-                // Just click on the canvas, do nothing
-                if (!brush || ((_a = brush.points) === null || _a === void 0 ? void 0 : _a.length) === 0) {
-                    return;
-                }
                 api.runAtNextTick(() => {
                     api.updateNode(brush, { visibility: 'hidden' }, false);
diff --git a/esm/App.js b/esm/App.js
--- a/esm/App.js
+++ b/esm/App.js
@@ -157,6 +157,8 @@
             this.world = yield World.create({
                 // Multithreading is not supported yet.
                 threads: 1,
+                maxEntities: 100000,
+                maxLimboComponents: 100000,
             });
             const tick = () => __awaiter(this, void 0, void 0, function* () {
                 yield this.world.execute();
diff --git a/lib/App.js b/lib/App.js
--- a/lib/App.js
+++ b/lib/App.js
@@ -162,6 +162,8 @@
             this.world = yield becsy_1.World.create({
                 // Multithreading is not supported yet.
                 threads: 1,
+                maxEntities: 100000,
+                maxLimboComponents: 100000,
             });
             const tick = () => __awaiter(this, void 0, void 0, function* () {
                 yield this.world.execute();

---
description: 项目通用开发规范，覆盖目录结构、TypeScript、异步、注释、前端体验、Rsbuild 工作流与 Skills
alwaysApply: true
---

# 项目规范

## 技术栈

- **框架**: React 18
- **语言**: TypeScript 5
- **构建工具**: Rsbuild（Rspack）
- **包管理器**: pnpm
- 优先遵循仓库已有目录结构、命名方式、组件模式和工具链配置。
- 修改 Rsbuild、Rspack、构建脚本、资源处理或性能相关内容时，先参考项目内文档与配置（`rsbuild.config.ts`、`AGENTS.md`）。
- 对外文档（人工查阅或与 Agent 对齐语境）：
  - 设计与界面原则：[Impeccable Style](https://impeccable.style/)（搭配 `.agents/skills/impeccable/`）
  - React API、Hooks 与规则：[React Reference](https://react.dev/reference/react)
  - Rsbuild 能力与配置：[Rsbuild 指南](https://rsbuild.rs/guide/start/)、[Rsbuild LLM 文档](https://rsbuild.rs/llms.txt)、[Rspack LLM 文档](https://rspack.rs/llms.txt)

## 当前目录

- 所有读写、生成和命令执行默认以**本仓库根目录**为工作区。
- 不要把业务源码、文档、脚本或资源生成到 `.git/`、`node_modules/` 或仓库外。
- 新增文件时优先放入已有约定目录；无明确目录时，按下列职责选择最小合适位置。
- 不要生成临时文件、调试产物、备份文件或一次性脚本并遗留在仓库中，除非它们是交付内容的一部分。

| 路径 | 职责 |
|------|------|
| `.cursor/rules/` | Cursor 项目规则（`.mdc` 格式） |
| `.cursor/skills/`、`/.agents/skills/` | Agent Skills（每 skill 独立目录 + `SKILL.md`） |
| `.agents/skills/impeccable/` | 前端设计、审查、打磨、适配、动效、可访问性与体验优化 |
| `plugins/` | Rsbuild 自定义插件；变更须同步 `plugins/README.md` |
| `public/` | 静态资源（`index.html` 模板引用、`bgm/` 音频、`*.ics` 日历等） |
| `src/` | 应用源码 |
| `src/App.tsx`、`src/App.css` | 根布局、随机/紧凑布局、设计系统 CSS 变量 |
| `src/index.tsx` | 应用入口与挂载 |
| `src/env.d.ts` | 环境类型声明 |
| `src/components/` | 可复用 UI 与业务卡片组件（见下方组件结构） |
| `src/config/` | 页面内容配置（`content.ts`）、构建产物 JSON（`blog-data.json`、`bgm-data.json`） |
| `src/hooks/` | 共享 Hooks（断点、随机布局、主题等） |
| `src/providers/` | React Context Provider（如 `ThemeProvider`） |
| `README.md` | 项目说明与功能概览 |
| `CHANGELOG.md` | 版本与功能迭代记录 |
| `AGENTS.md` | Agent 常用命令摘要 |
| `rsbuild.config.ts` | Rsbuild 与插件配置 |
| `package.json`、`pnpm-lock.yaml` | 依赖与脚本 |

复杂组件（如 `WorldMap/`）可在目录内增加 `constant.ts`、`types.ts`、工具模块等，但须保持职责清晰，不破坏对外 `index.ts` 导出约定。

## 常用命令

```bash
pnpm dev        # 启动开发服务器（自动打开浏览器）
pnpm dev:noopen # 启动开发服务器（不打开浏览器）
pnpm build      # 构建生产版本
pnpm preview    # 预览生产构建
pnpm lint       # 运行 ESLint 检查
pnpm format     # 使用 Prettier 格式化代码
```

不确定命令时参考根目录 `AGENTS.md`。

## 组件结构

公共组件维护在 `src/components/`，遵循以下规范（**本仓库使用 `ComponentName.tsx`，不使用 `index.tsx` 作为组件实现文件名**）：

```
src/components/
└── ComponentName/
    ├── ComponentName.tsx  # 组件实现
    ├── ComponentName.css  # 组件样式
    └── index.ts           # 导出入口
```

新增卡片或模块时，同步更新 `src/config/content.ts`（及 `content.example.ts`）、`App.tsx` 布局配置，并在 `README.md` / `CHANGELOG.md` 记录大功能变更。

## 依赖库

| 库名 | 用途 | 文档 |
|------|------|------|
| lucide-react | 图标库 | https://lucide.dev/ |
| Canvas 世界地图 | 旅行足迹底图（plane-list 方案） | https://github.com/SHUAXINDIARY/plane-list/blob/main/docs/map.md |
| dayjs | 日期处理 | https://day.js.org/ |
| ical.js | iCal 日历解析 | https://github.com/kewisch/ical.js |
| motion | React 动画库 | https://motion.dev/docs/react |

## TypeScript 规范

- 类型须完整声明；禁止用 `any` 作为逃避建模的兜底类型。
- 函数参数、返回值、组件 props、事件处理器和异步结果须有明确类型。
- 优先使用 `interface` 或具名 `type` 表达业务语义，避免在复杂位置堆叠匿名类型。
- 类型收窄通过明确判断、联合类型或标准 API 完成，避免无依据的强制断言。
- 所有对外暴露的类型定义须有注释：`interface`、具名 `type`、`enum` 及其中**每个属性/成员**均须说明语义与用途（与下方注释规范一致）。
- 由 `Pick` / `Omit` 等从已注释类型导出且未新增字段时，可在该类型上注明「字段语义与源类型 `X` 一致」。

## 异步与数据流

- 异步代码统一使用 `async` / `await`，不要混用 `.then()` 链式写法。

```typescript
// ❌ BAD
function fetchData() {
  return fetch('/api/data')
    .then((res) => res.json())
    .then((data) => processData(data));
}

// ✅ GOOD
async function fetchData() {
  const res = await fetch('/api/data');
  const data = await res.json();
  return processData(data);
}
```

- 每个异步边界须明确处理加载、成功、失败和空数据状态。
- 错误处理保留上下文，避免静默吞错或只打印无意义日志。
- 不要在渲染逻辑中直接触发副作用；副作用放在事件处理、`useEffect` 或明确的服务函数中。

## 函数与状态

- 函数尽量遵循纯函数原则；有副作用的函数通过命名或注释表达意图。
- 单个函数只承担一个清晰职责；分支或嵌套过深时拆分为具名辅助函数。
- 多步骤、分支或异步函数须在函数内用分段注释说明阶段目的与关键边界。
- 状态更新保持不可变数据模式，不直接修改已有对象或数组。

## 常量与注释

- 文件级常量集中在文件顶部（`import` 之后），业务含义不直观的常量须简短注释。

```typescript
import { useState } from 'react';

/** 默认分页大小 */
const DEFAULT_PAGE_SIZE = 10;
/** 请求超时（毫秒） */
const API_TIMEOUT_MS = 5000;
```

- 魔法数字、固定文案、路由路径、存储 key、正则等提取为具名常量。
- 所有逻辑变量、Hook 状态、函数及 TypeScript 类型/属性均须有注释；说明目的与取舍，避免描述表面行为的低价值注释。

```typescript
/** 用户信息类型 */
interface UserInfo {
  /** 用户唯一标识 */
  id: string;
  /** 用户显示名称 */
  name: string;
  /** 用户头像 URL */
  avatar?: string;
}

/** 当前页码，从 1 开始 */
const [page, setPage] = useState(1);
```

## 前端实现

- 关注可访问性：语义化 HTML、键盘可用、合理的 `aria` 文案与足够对比度。
- 组件须适配移动端/平板/桌面（`useBreakpoint`、紧凑布局与随机布局并存）。
- 处理文案溢出、空状态、错误状态、加载状态与极端数据量。
- 样式优先复用 `App.css` 中的设计 token（`--color-*`、`--spacing-*`、`--radius-*` 等）及现有组件模式，避免引入不一致的视觉系统。
- 界面设计、审查或体验优化任务优先遵循 `.agents/skills/impeccable/`。

## 生态管理

- Rsbuild 自定义插件全部放在 `plugins/` 目录。
- 每次新增或修改插件，须在 `plugins/README.md` 中记录用途、配置项与输出路径。

## 文档

- 大功能迭代与更新，自动同步记录到根目录 `README.md` 和 `CHANGELOG.md`。
- 配置项说明维护在 `src/config/README.md`；组件说明维护在 `src/components/README.md`。
- 若使用 Impeccable 工作流，可在根目录维护 `PRODUCT.md` / `DESIGN.md` 作为产品与视觉上下文（可选）。

## 验证与交付

- 修改后优先运行与改动相关的 `pnpm lint`、`pnpm build`；必要时 `pnpm format`。
- 不提交无关格式化、锁文件或配置变更；不要回滚用户已有改动。
- 交付前对本次修改过的文件使用 `ReadLints` 检查。

## 代码质量工具

### ESLint

项目使用 ESLint 9 + TypeScript ESLint，包含：

- `eslint-plugin-react-hooks`：React Hooks 规则
- `eslint-plugin-react-refresh`：React Fast Refresh 兼容性

### Prettier

使用 Prettier 格式化；提交前运行 `pnpm format`。

## 启用 Skills

- Cursor 会自动发现 `.agents/skills/`、`.cursor/skills/` 下包含 `SKILL.md` 的 skill。
- 每个 skill 须位于独立目录，`SKILL.md` frontmatter 声明 `name` 与 `description`；`name` 与目录名一致。
- 需要按路径限制 skill 时，优先使用 `paths` frontmatter，新 skill 勿优先使用旧版 `globs`。
- 规则细节参考：[Cursor Skills 文档](https://cursor.com/docs/skills)。

| 场景 | 优先使用的 Skill |
|------|------------------|
| React 性能、渲染与数据获取优化 | `.cursor/skills/vercel-react-best-practices/` |
| 前端界面设计、打磨、适配、动效、审计 | `.agents/skills/impeccable/` |
| 创建或维护 Cursor 规则 | `create-rule` skill（`.mdc` 格式） |

处理相关任务时须**实际读取并遵循**对应 `SKILL.md`，不要仅凭记忆套用。
